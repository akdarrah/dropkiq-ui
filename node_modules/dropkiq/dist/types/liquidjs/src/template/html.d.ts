import { Template } from '../template/template';
import { ITemplate } from '../template/itemplate';
import { HTMLToken } from '../parser/html-token';
import { Context } from '../context/context';
import { Emitter } from '../render/emitter';
export declare class HTML extends Template<HTMLToken> implements ITemplate {
    value: import("./value").Value;
    private str;
    constructor(token: HTMLToken);
    render(ctx: Context, emitter: Emitter): IterableIterator<void>;
}
