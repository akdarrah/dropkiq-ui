import { ITemplate } from './liquidjs/src/template/itemplate';
import { Context } from './liquidjs/src/context/context';
import { Token } from './liquidjs/src/parser/token';
import { DataSchema } from './DataSchema';
export declare class ScopeCapture {
    private renderer;
    private templates;
    private caretToken;
    private dataSchema;
    constructor(templates: ITemplate[], caretToken: Token, dataSchema: DataSchema);
    optionsFromScope(): object;
    getCapturedContext(): Context;
    getCapturedRendered(): string;
    capture(scope?: object): boolean;
    private classifyDataType;
}
